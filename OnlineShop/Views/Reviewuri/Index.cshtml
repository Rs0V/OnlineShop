@{
	ViewBag.Titlu = "Lista review-uri";
}
<br />
<h2 class="text-center">@ViewBag.Titlu</h2>
<br />

@if (TempData.ContainsKey("message"))
{
	<h1>@ViewBag.message</h1>
}

<br />

<div class="align-items-center justify-content-center d-flex">
	<a class="btn btn-success" href="/Reviewuri/New">Adauga un nou review</a>
</div>

<br />

@foreach (var review in ViewBag.Reviewuri)
{
	@Html.Label("Id Utilizator", "Review utilizator")
	<p>@ViewBag.Utilizatori.ToList<Utilizator>().Find(review.UtilizatorId).Nume @ViewBag.Utilizatori.ToList<Utilizator>().Find(review.UtilizatorId).Prenume
	</p>

	@Html.Label("Id Produs", "Review produs")
	<p>@ViewBag.Produse.ToList<Produs>().Find(review.ProdusId).Titlu</p>

	@Html.Label("Rating", "Rating review")
	@for (int i = 1; i <= 5; ++i)
	{
		@if (i <= review.Rating)
		{
			// Stele aurii
			// De pus inline-block
			<img src="" />
		}
		else
		{
			// Stele gri
			// De pus inline-block
			<img src="" />
		}
	}

	@Html.Label("Continut", "Continut review")
	<p>@review.Continut</p>
	

	<a class="btn alert-success" href="/Reviewuri/Show/@review.Id">Afisare review</a>

	@if (ViewBag.userManager.GetUserId(User) == review.UtilizatorId || User.IsInRole("Administrator"))
	{
		<a class="btn alert-success" href="/Reviewuri/Edit/@review.Id">Editare review</a>
	}
	
	<br />
	<hr />
	<br />
}
<br />
